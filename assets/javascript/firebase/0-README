
SO.

These loose js files are snippets of code that do things.

--  signout.js  :   on click event that you can attach to a button to sign user out.

--  debug-login-data.js :   can be ignored.

--  push-data-firebase.js   :   push user's data to the database

--  push-data-session.js    :   push user's data into sessionStorage

--  update-local-data.js    :   retrieve and store data from database into sessionStorage

